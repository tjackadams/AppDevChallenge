import React, { PureComponent } from "react";

import { AlarmTable } from "../components/AlarmTable";

class AlarmContainer extends PureComponent {
  render() {
    return <AlarmTable />;
  }
}

export default AlarmContainer;
